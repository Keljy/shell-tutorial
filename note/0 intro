主题 1: The Shell
shell 是什么？
文字接口 核心功能：它允许你执行程序，输入并获取某种半结构化的输出。

Bourne Again SHell 简称 “bash” 

shell 基于空格分割命令并进行解析，然后执行第一个单词代表的程序，并将后续的单词作为程序可以访问的参数。如果您希望传递的参数中包含空格（例如一个名为 My Photos 的文件夹），您要么用使用单引号，双引号将其包裹起来，要么使用转义符号 \ 进行处理（My\ Photos）。

类似于 Python 或 Ruby，shell 是一个编程环境，所以它具备变量、条件、循环和函数。
在shell中导航
绝对路径:以/开头
相对路径:相对于当前工作目录的路径,. 表示的是当前目录，而 .. 表示上级目录
常用命令：
	- pwd 获取当前工作目录
	- cd 切换目录
	- ls 查看指定目录下包含哪些文件
	- mv 用于重命名或移动文件 
	- cp 拷贝文件
	- mkdir 新建文件夹
	- man 接受一个程序名作为参数，然后将它的文档（用户手册）展现给您
在程序间创建连接

重定向
< file
> file 
使用 >> 来向一个文件追加内容
